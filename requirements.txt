# Install locally only — these are needed by scripts/build_data.py to
# (re)generate the parquets. Streamlit Cloud doesn't need them because
# it just reads the committed parquets.
#
#     pip install -r requirements-dev.txt
#     python scripts/build_data.py

-r requirements.txt

nfl_data_py>=0.3.2
numpy>=1.26.0
